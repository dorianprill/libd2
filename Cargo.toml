[package]
name = "libd2r"
version = "0.1.0"
description = """A Diablo II: Resurrected library for core and simple client functionality, 
                 written in Rust for performance, safety and re-usability."""
authors = ["Dorian Prill"]
repository = "https://github.com/dorianprill/libd2r"
readme = "README.md"
keywords = ["diablo", "resurrected"]
categories = ["games"]
edition = "2021"
license = "MIT"


exclude = [
    "*.png",
    "*.jpg",
    "examples/*",
    ".github/*",
    ".gitignore",
    ".vscode/*",
]


[dependencies]
bzip2 = "0.6"
flate2 = "1"
pklib = "0.2"
netdev = "0.31"
etherparse = "0.14"
socket2 = { version = "0.5", features = ["all"] }
windows-sys = { version = "0.52", features = ["Win32_Networking_WinSock"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
sha2 = "0.10"
